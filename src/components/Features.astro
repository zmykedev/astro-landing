---
---
<section class="features">
  <div class="container">
    <h2 class="section-title">Why Lumina?</h2>
    <div class="features-grid">
      <div class="feature reveal delay-1">
        <div class="icon-wrapper">
          <div class="icon">✦</div>
        </div>
        <h3>Ultra High Resolution</h3>
        <p>Capture every detail with our 100MP sensors.</p>
      </div>
      <div class="feature reveal delay-2">
        <div class="icon-wrapper">
          <div class="icon">☀</div>
        </div>
        <h3>Dynamic Range</h3>
        <p>Experience true-to-life colors and lighting.</p>
      </div>
      <div class="feature reveal delay-3">
        <div class="icon-wrapper">
          <div class="icon">☾</div>
        </div>
        <h3>Low Light Mastery</h3>
        <p>Perfect shots even in the darkest environments.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    padding: var(--spacing-section) 0;
    background-color: var(--color-bg);
    position: relative;
    z-index: 1;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 4rem;
    text-align: center;
  }

  .feature {
    padding: 3rem 2rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 24px;
    transition: transform 0.4s ease, background-color 0.4s ease, box-shadow 0.4s ease;
  }

  .feature:hover {
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    border-color: rgba(255, 255, 255, 0.1);
  }

  .icon-wrapper {
    width: 80px;
    height: 80px;
    margin: 0 auto 2rem;
    background: linear-gradient(135deg, #333, #111);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 10px 20px rgba(0,0,0,0.3);
    position: relative;
  }

  .icon-wrapper::after {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 50%;
    padding: 2px;
    background: linear-gradient(to bottom right, #555, transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
  }

  .icon {
    font-size: 2.5rem;
    color: var(--color-primary);
    text-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
  }

  .feature h3 {
    font-size: 1.8rem;
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .feature p {
    color: var(--color-secondary);
    font-size: 1.1rem;
  }

  .delay-1 { transition-delay: 0.1s; }
  .delay-2 { transition-delay: 0.3s; }
  .delay-3 { transition-delay: 0.5s; }
</style>
